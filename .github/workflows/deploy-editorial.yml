name: Deploy editorial portfolio preview

on:
  push:
    branches:
      - design/portfolio-v3-editorial
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: editorial-preview
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy build output
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages-v3
          publish_dir: ./dist
          force_orphan: true
